version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        emit_json_tags: True
        emit_empty_slices: True
        out: "internal/database"
